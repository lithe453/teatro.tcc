CREATE TABLE sala (
    id_sala SERIAL PRIMARY KEY,
    nome VARCHAR(255) NOT NULL
);

CREATE TABLE assento (
    id_assento SERIAL PRIMARY KEY,
    id_sala INTEGER NOT NULL,
    fila INTEGER NOT NULL,
    lugar INTEGER NOT NULL,

    FOREIGN KEY (id_sala)
        REFERENCES sala(id_sala),

    UNIQUE (id_sala, fila, lugar)
);

CREATE TABLE reserva (
    id_reserva SERIAL PRIMARY KEY,
    id_assento INTEGER NOT NULL UNIQUE,
    data_reserva TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (id_assento)
        REFERENCES assento(id_assento)
);

INSERT INTO sala (nome)
VALUES
('Sala 1'),
('Sala 2'),
('Sala 3');

INSERT INTO assento (id_sala, fila, lugar)
SELECT
    sala,
    fila,
    lugar
FROM
    generate_series(1, 3) AS sala,
    generate_series(0, 9) AS fila,
    generate_series(0, 19) AS lugar;
